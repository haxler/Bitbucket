@page "/products/edit/{Id:int}"

<h3>Editar Productos</h3>

@if (product is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="productForm" Label="Productos" Model="product" OnValidSubmit="EditAsync" ReturnAction="Return" />
}